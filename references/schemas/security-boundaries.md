# Security Boundaries

Do not store secrets. Safe outputs include file paths, variable names, dependency names, and access patterns. Unsafe outputs include password values, tokens, private keys, certificate material, production connection strings, and customer data exports.
